// Configuration for API vs Mock data
// Set to true to use real API, false to use mock data
export const USE_REAL_API = false

// API Base URL
export const API_BASE_URL = '/api'

// Institution ID (will be set from context when using real API)
export const DEFAULT_INSTITUTION_ID = 'mock-institution-1'

// Academic Year ID
export const DEFAULT_ACADEMIC_YEAR_ID = 'mock-year-2025'
