{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/api && npx prisma generate --schema=prisma/schema.prisma && npm run build"
  },
  "deploy": {
    "startCommand": "cd apps/api && npx prisma migrate deploy && npm run seed:production && npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
