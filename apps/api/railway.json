{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/api && npx prisma validate --schema=prisma/schema.prisma && npx prisma generate --schema=prisma/schema.prisma && npm run build"
  },
  "deploy": {
    "startCommand": "cd apps/api && npx prisma migrate deploy && node --max-old-space-size=384 dist/src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
